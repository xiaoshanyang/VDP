{
  apps: [{
    name: "VDP",
    script: "./app.js",
    instances: 1,
    exec_mode: "cluster",
    watch: true,
    ignore_watch: ["public", "middlewares/data", "logs", ".idea", "dump.rdb"],
    env: {
      "NODE_ENV": "development"
    },
    env_production: {
      "NODE_ENV": "production"
    }
  },{
    name: "VDP-API",
    script: "./app_service.js",
    instances: 1,
    exec_mode: "cluster",
    watch: true,
    ignore_watch: ["public", "middlewares/data", "logs", ".idea", "dump.rdb"],
    env: {
      "NODE_ENV": "development"
    },
    env_production: {
      "NODE_ENV": "production"
    }
  }]
}